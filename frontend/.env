# Works around a known react-scripts 5.0.1 + eslint-plugin-jest / @typescript-eslint
# transitive dependency-version conflict ("Environment key jest/globals is unknown")
# that breaks compilation on fresh installs. This only disables the optional
# in-browser ESLint warnings overlay — it does not affect app behavior.
DISABLE_ESLINT_PLUGIN=true
